# 로드맵

한 번에 한 단계씩. 단계를 건너뛰지 않는다. 특히 v1을 생략하고 v2로 가면
버그가 게임 로직 문제인지 네트워크 문제인지 구분할 수 없게 된다.

## v1 — 한 기기 로컬 대전

목표: 게임이 실제로 재미있는지 검증한다. 서버 없음.

- [x] Vite + TypeScript(strict) 프로젝트 초기화, vitest 설정
- [x] `config.ts` — 공정성 상수 전체 정의
- [x] `core/GameState.ts` — 보드 상태, 소유권 전환, 점수, 라운드/매치 진행
- [x] `tests/GameState.test.ts` — 카드 뺏기, 라운드 종료 판정, 매치 승리 조건
- [x] `input/InputGovernor.ts` — pointer 이벤트 필터링
- [x] `tests/InputGovernor.test.ts` — 포인터 개수 제한, 쿨다운, 카드 락아웃,
      손바닥 크기 거부, 마우스 입력 거부
- [x] `render/Renderer.ts` — 5x5 보드, 카드 상/중립/하 분할, 소유 색상 + 패턴
- [x] HUD — 양쪽 점수, 라운드 타이머, 라운드 표시
- [x] 준비 버튼 홀드 → 3-2-1 카운트다운 → 부정 출발 페널티
- [x] 라운드 종료 / 매치 종료 화면, 라운드 간 보드 180도 회전
- [x] 카드 물리 크기 고정 CSS, 대상 해상도 4종 검증
      (1366x768 / 1920x1080 / iPad 세로 768x1024 / iPad 가로 1024x768 에서
       카드 16.0mm 유지, 오버플로 없음 — 데스크톱 브라우저 뷰포트 기준)
- [x] 키보드 조작 경로 (준비 / 시작 / 재시작)
- [x] PWA 매니페스트 + 아이콘 + 최소 서비스 워커 (원래 v4 항목이나 v1 에서 처리)
- [ ] **실제 태블릿에서 2인 플레이 테스트** — 여기서 상수 튜닝
      (남은 항목. `docs/TESTING.md` 의 체크리스트를 따라간다)

### v1 완료 기준

두 사람이 실제 태블릿으로 3판을 끝까지 하고, 판정에 대한 불만이 나오지 않는다.

## v1.5 — 시작 설정 · 폰 모드 · NPC · 시간 선택

목표: 상대가 없어도 혼자 할 수 있고, 폰에서도 할 수 있게.

- [x] 시작 설정 화면 (기기 / 상대 / 라운드 시간), 키보드 조작, 마지막 선택 기억
- [x] 폰 모드 — 카드 13mm, HUD 압축, `device` 프로필로 분리
- [x] 라운드 시간 30초 / 1분 / 2분 선택, `GameState` 에 주입
- [x] `core/NpcPlayer.ts` — 5단계 실력, DOM·시계 의존 없음, 난수 주입
- [x] `InputGovernor.submitTap` — NPC 탭도 쿨다운·락아웃 적용
- [x] `tests/NpcPlayer.test.ts` — 예약 시각 사용, 속도 상한, 백로그 폐기, 카드 선택
- [x] 보드 위치 드리프트 자동 재측정 (HUD 높이 변화로 중립 띠 판정이 밀리던 버그)
- [ ] 실기기에서 NPC 5단계 체감 난이도 튜닝 (`config.npc.tiers`)

## v2 — 태블릿 2대 온라인 대전

목표: `GameState`를 서버로 이식하고 서버 권위 구조로 전환.

- [x] Spring Boot 4.1 / Java 25 / Gradle 프로젝트 (`server/`), 순수 WebSocket 핸들러 `/ws`
- [x] 방 코드 6자리 생성 / 입장 / 만석·없는 방 오류 / 2명 충족 시 게임 화면
- [x] `GameState`를 서버 측으로 이식 (`server/src/main/java/touchflip/core/GameState.java`)
      + TS 테스트를 JUnit 으로 포팅해 두 구현의 동치 검증 (`GameStateTest`, 27개)
- [x] 메시지 프로토콜 구현 (GAME_SPEC 7절, `Messages.java` ↔ `src/net/protocol.ts`)
- [x] 클라이언트 `net/Connection.ts` (자동 재접속) · `net/RemoteState.ts` (서버 상태 거울, 타이머 보간)
      · `net/RemoteSession.ts` (방/토큰/상태)
- [x] 온라인 모드 카드 분할 제거 (`zoneMode: 'full'`), 내 HUD 가 항상 아래
- [x] 방 상태 동시 접근 — `Room` 의 메서드 전부 `synchronized`, 같은 틱 안의 동시 탭은 먼저 잡힌 락 순서 (`RoomTest`)
- [x] 서버 측 쿨다운·락아웃 재적용 (`ServerInputGovernor`)
- [x] 연결 끊김 5초 유예(토큰 재접속) 및 부전승 처리 (`RoomTest`, 브라우저 2탭으로 확인)
- [x] 시작 화면을 단계식 마법사로 재구성 (기기 → 상대 → 세부 → 시간)
- [ ] 같은 Wi-Fi 환경에서 2대 실기기 테스트

## 과제 제출용 추가 작업 (싱글플레이 트랙)

목표: 대학 과제 체크리스트(T02-C01~C31)를 싱글플레이(NPC) 트랙으로 충족한다.
온라인 멀티는 서버가 필요해 "무로그인 공개" 요구와 충돌하므로 채점 범위에서 뺀다.

- [x] 시작 마법사에 싱글플레이/멀티플레이 상위 분류 추가
- [x] 개인 최고기록 랭킹 (`core/Records.ts`, localStorage, 서버 불필요)
- [x] 일시정지/재개 (`GameState.extendDeadlines`, P 키 + 버튼)
- [x] 효과음(매치 승/패, Web Audio 합성) + 음소거 · 모션감소 토글, 둘 다 저장됨
- [x] 화면에 규칙/조작 문구 명시 (설정 화면 태그라인 + 게임 idle 오버레이)
- [x] GitHub Pages 배포 워크플로 (`.github/workflows/deploy-pages.yml`) 준비
- [ ] 실제 GitHub 저장소 생성 + push + Pages 활성화 (계정 필요, 사용자가 직접)
- [ ] 1366×768 / 1920×1080 재검증, 10분 연속 실행 콘솔 오류 검사
- [ ] NPC 난이도 값 하나를 골라 전/후 10회씩 플레이 기록 비교
- [ ] 확인 방법 4줄 + AI/본인 판단 3줄 제출문 작성

## v3 — 지연 보상

목표: 핑이 낮은 쪽이 이기는 문제를 제거한다. 포트폴리오의 핵심.

- [ ] `net/ClockSync.ts` — ping-pong 20회, RTT 최소 샘플로 오프셋 추정
- [ ] 탭 전송 시 서버 기준 추정 시각 첨부
- [ ] 서버 120ms 판정 윈도우 — 버퍼링 후 `tapTime` 정렬 처리
- [ ] 클라이언트 예측 렌더링 + 서버 확정 시 롤백
- [ ] 타임스탬프 조작 방어 (±150ms 허용 오차)
- [ ] 매치 시작 전 RTT 측정 및 편차 80ms 초과 시 시작 차단
- [ ] 인위적 지연을 주입해 공정성 검증하는 테스트
      (한쪽에 100ms 지연을 걸고도 먼저 누른 쪽이 이기는지)

### v3 완료 기준

한쪽 클라이언트에 100ms 인위적 지연을 걸어도, 먼저 탭한 쪽이 카드를 가져간다.

## v4 — 배포 및 포트폴리오 정리

- [ ] GitHub Pages 정적 배포 (프론트)
- [ ] 서버 배포. 무료 티어는 콜드 스타트가 30초 걸릴 수 있으므로
      README에 명시하거나 데모 GIF로 대체
- [x] PWA 매니페스트 — 태블릿 홈화면 추가 (v1 에서 완료)
- [ ] README 한국어 / 일본어 2개국어. 최상단에 데모 GIF와 링크
- [ ] 설계 판단 기록 문서 — 왜 웹을 선택했는지, 시계 동기화를 어떻게
      처리했는지. 면접에서 그대로 쓸 수 있는 형태로
