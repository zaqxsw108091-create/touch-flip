# AGENTS.md

## 프로젝트

터치 기반 2인 대전 카드 뒤집기 게임. 5x5 = 25장의 카드를 더 빨리 자기 색으로
뒤집는 쪽이 이긴다. 태블릿 및 터치 지원 노트북 대상.

**핵심 설계 철학: 기기 성능 차이가 승부에 영향을 주면 안 된다. 오직 손가락
속도만이 결과를 결정해야 한다.** 모든 설계 판단은 이 원칙을 기준으로 한다.

## 문서

- `docs/GAME_SPEC.md` — 게임 규칙, 입력 공정성 상수, 네트워크 프로토콜
- `docs/ROADMAP.md` — 단계별 작업 목록. 현재 어느 단계인지 여기서 확인할 것

## 기술 스택 (변경 금지)

- Vanilla TypeScript (strict mode). React/Vue 등 프레임워크 사용 금지
- 빌드: Vite. 정적 사이트로 GitHub Pages 배포 가능해야 함
- 렌더링: DOM + CSS. 카드 25장 규모에서 Canvas는 과잉
- 서버(v2 이후): Spring Boot + 순수 WebSocket (STOMP 사용 안 함)
- 런타임 외부 라이브러리 금지. 개발 의존성(vitest 등)만 허용

## 절대 규칙

1. **`GameState`는 DOM에 의존하지 않는다.** 순수 TypeScript 클래스로 작성하고
   유닛 테스트가 가능해야 한다. `document`, `window` 참조 금지.
2. **모든 입력은 `InputGovernor`를 거친다.** 게임 로직이 pointer 이벤트를 직접
   구독하는 코드가 하나라도 생기면 공정성 보장이 무너진다.
3. **튜닝 상수는 절대 하드코딩하지 않는다.** 전부 `src/config.ts`의 단일 객체에
   모은다. 매직 넘버가 로직 안에 박혀 있으면 안 된다.
4. **애니메이션은 게임 상태를 블로킹하지 않는다.** 카드 뒤집기 연출은 순수
   시각 효과이며, 상태는 입력 즉시 반영된다.
5. **판정 시각은 `event.timeStamp`를 쓴다.** `requestAnimationFrame` 기준으로
   판정하면 120Hz 기기가 60Hz 기기보다 유리해진다.
6. **햅틱(진동) 사용 금지.** 기기별 반응 편차가 커서 공정성을 해친다.

## 작업 방식

- 새 단계를 시작할 때는 **먼저 파일 구조와 인터페이스 설계를 제안하고 승인을
  받은 뒤** 구현한다. 승인 없이 파일을 대량 생성하지 않는다.
- `docs/ROADMAP.md`의 한 단계씩만 진행한다. 여러 단계를 한 번에 처리하지 않는다.
- 단계를 완료하면 ROADMAP의 체크박스를 갱신한다.
- 공정성에 영향을 주는 판단(입력 처리, 타이밍, 네트워크 순서)을 내릴 때는
  왜 그렇게 했는지 코드 주석이 아니라 대화로 먼저 설명한다.

## 명령어

```bash
npm run dev      # 개발 서버 (모바일 테스트용으로 --host 옵션 포함)
npm run build    # 프로덕션 빌드
npm run test     # 유닛 테스트
npm run preview  # 빌드 결과 로컬 확인
```

## 테스트 환경 주의사항

개발자는 데스크톱 브라우저에서 코드를 작성하지만 **실제 대상은 터치 기기**다.
- 마우스로는 재현되지 않는 버그가 많다. Chrome DevTools의 터치 에뮬레이션 및
  실제 태블릿 양쪽에서 확인이 필요한 부분은 명시적으로 알려줄 것
- `npm run dev`는 반드시 `--host`로 띄워 같은 Wi-Fi의 태블릿에서 접속 가능해야 함
